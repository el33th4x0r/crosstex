sudo: required
language: python

before_install:
    - sudo apt-get -qq update
    - sudo apt-get install python3-ply texlive-xetex -y

install:
    - sudo ./setup.py install

script:
    - ./run-tests.py
